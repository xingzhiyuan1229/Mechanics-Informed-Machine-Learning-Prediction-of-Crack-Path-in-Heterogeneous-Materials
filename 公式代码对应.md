# 公式代码对应

> 说明：部分相场方程在仓库中仅提供 MEF90 输入/日志文件，求解器实现不随库提供。以下按论文原章节组织，逐条给出“最低层可访问代码”链接与完整片段，之后列出对应公式（代码在前、公式在后）。

## 2. Computational framework

### 2.0 相场能量泛函与损伤变量（对应 Eq. (1)–(3)）

**代码链接**：`FE_code/dp.yaml#L1-L27`
```yaml
verbose: 1
boundaryDisplacement_scaling: linear
defmech:
    irrevthres: 0.
    timestepping:
        type: quasistatic
heatxfer:
    timestepping:
        type: null
temperature:
    offset: 0
time:
    min: 0
    max: 1
    numstep: 100
cs0001:
    fracturetoughness:    0.1000000000
    residualstiffness: 1e-6
    internalLength: 1.25
    damage:
        type: AT1
    plasticity:
        type: NONE
    HookesLaw:
        type: isotropic
        YoungsModulus:    1.0000000000
        vsd_poissonratio:  0.3
```

**代码链接**：`FEM-generated_training_datasets/generation_log.txt#L19-L28`
```text
FEM SIMULATION PARAMETERS
================================================================================
Damage Model: AT1 (Ambrosio-Tortorelli type 1)
Fracture Toughness (Gc): 0.1
Residual Stiffness (k): 1.0e-6
Internal Length Scale (ℓ₀): 1.25
Constitutive Law: Isotropic Hooke's Law
Young's Modulus (E): 1.0
Poisson's Ratio (ν): 0.3
Plasticity Model: NONE
```

公式：
- $\Omega \subset \mathbb{R}^d$
- $\epsilon$
- $\Gamma$
- $\mathbf{u}$
- $\psi (\epsilon (\mathbf{u}))$
- $G_{c}$
- $\mathcal{H}^{d - 1}$
- $(d - 1)$
- $d(\mathbf{x})\in [0,1]$
- $d(\mathbf{x}) = 0$
- $d(\mathbf{x}) = 1$
- $\ell_0$
- $\mathcal{G}(\Gamma)$
- $\ell_0 \to 0$
- $E_{\ell_0}(\mathbf{u},d)$
- $\psi (\epsilon) = \frac{1}{2}\epsilon :\mathbb{C}:\epsilon$
- $g(d)$
- $g(d) = (1 - d)^2 +k$
- $k\ll 1$
- $\sigma = g(d)\frac{\partial\psi}{\partial\epsilon}$
- $1 - d$
- $(\mathbf{u},d)$
- $\dot{d}(\mathbf{x},t)\geq 0$
- $d_{n+1}$
- $d_{n}$

$$
\mathcal{E}(\mathbf{u},\Gamma) = \int_{\Omega \backslash \Gamma}\psi (\epsilon (\mathbf{u}))\mathrm{d}\mathbf{x} + G_{c}\mathcal{H}^{d - 1}(\Gamma) \qquad (1)
$$

$$
G_{c}\int_{\Gamma}dH^{d - 1}\approx \min_{d}\int_{\Omega}G_{c}\left(\frac{d^{2}}{2\ell_{0}} +\frac{\ell_{0}}{2}|\nabla d|^{2}\right)\mathrm{d}\mathbf{x}. \qquad (2)
$$

$$
E_{\ell_0}(\mathbf{u},d) = \int_{\Omega}g(d)\psi (\epsilon (\mathbf{u}))\mathrm{d}\mathbf{x} + \int_{\Omega}G_c\left(\frac{(1 - d)^2}{2\ell_0} +\frac{\ell_0}{2}|\nabla d|^2\right)\mathrm{d}\mathbf{x} \qquad (3)
$$

### 2.0 变量刚度边界条件（VSBC）与域划分

**代码链接**：`README.md#L70-L73`
```text
- Stiffness gradient: `E_vsd(x) = (1 - x/L_x)^n * a + b`
- Parameters: `n=8, a=0.4, b=0.005`
- Domain dimensions: 200×80 (main), 200×5 (VSBC strips)
```

**代码链接**：`FE_code/dp.yaml#L24-L52`
```yaml
    HookesLaw:
        type: isotropic
        YoungsModulus:    1.0000000000
        vsd_poissonratio:  0.3
cs0002:
    fracturetoughness: 100.0
    residualstiffness: 1e-6
    internalLength: 1.25
    damage:
        type: AT1
    plasticity:
        type: NONE
    HookesLaw:
        type: isotropic
        YoungsModulus:    0.3453052090
        vsd_poissonratio:     0.0010000000
cs0003:
    fracturetoughness: 100.0
    residualstiffness: 1e-6
    internalLength: 1.25
    damage:
        type: AT1
    plasticity:
        type: NONE
    HookesLaw:
        type: isotropic
        YoungsModulus:    0.2935558316
        vsd_poissonratio:     0.0010000000
```

**代码链接**：`FE_code/dp.yaml#L628-L637`
```yaml
vs0001:
    displacementbc: yes,yes,no
    boundaryDisplacement: 0,-40.0000,0
    damageBC: yes
    boundaryDamage: 0.
vs0002:
    displacementbc: yes,yes,no
    boundaryDisplacement: 0,40.00000,0
    damageBC: yes
    boundaryDamage: 0.
```

公式：
- $\Omega_{\mathrm{d}}$
- $\Omega_{\mathrm{vsd}}$
- $E_{\mathrm{vsd}} = \left(1 - \frac{x}{L_x}\right)^n a + b$
- $x$
- $n=8$
- $a=0.4$
- $b=0.005$
- $G_c$
- $\ell_0 = 0.6$
- $h=0.25$
- $J$
- $a-b$
- $b-c$
- $c-d$

## 2.1. Generation of ML datasets

**代码链接**：`FEM-generated_training_datasets/generation_log.txt#L12-L18`
```text
ROI Dimensions: 9 × 4 (elements)
Available Pore Locations: 35 discrete positions
Domain Size (FEM): 11 × 6 (elements)
Initial Crack Length: 2 elements
Loading Type: Mode I (Uniaxial tension along y-axis)
Master Random Seed: 42 (used for all stochastic processes)
```

**代码链接**：`FEM-generated_training_datasets/generation_log.txt#L31-L45`
```text
DATASET 1: BASELINE TRAINING SET (631 CASES)
================================================================================
Description: Systematic enumeration of 0, 1, and 2 pore configurations
Total Cases: 631 (1 + 35 + 595)
ML Dataset Size: 1262 matrices (input + output pairs)
Maximum Local Porosity: 5.5% (2 pores in 9×4 ROI)

PORE CONFIGURATION BREAKDOWN
--------------------------------------------------------------------------------
Case Type              | Count | Pore Count | Combination Formula
--------------------------------------------------------------------------------
No pores              | 1     | 0          | 1
Single pore           | 35    | 1          | C(35,1) = 35
Two pores             | 595   | 2          | C(35,2) = 595
```

**代码链接**：`ML_code/train/train.py#L19-L28`
```python
DATA_DIR        = Path('../512')          # << directory with *_map*.txt
input_time_steps = 0
total_time_steps = 1
time_steps_interval = 1

initialCase = 1
finalCase   = 631 * 2

matrix_rows, matrix_cols = 9, 4           # every map is 9 x 4 = 36 pixels
feat_dim = matrix_rows * matrix_cols
```

**代码链接**：`ML_code/predict/ref_predict.py#L19-L21`
```python
matrix_rows = 9  # Number of rows in each matrix
matrix_cols = 4  # Number of columns in each matrix
```

公式：
- $11 \times 6$
- $9 \times 4$
- $\sigma_{yy}$
- $9 \times 4$（文中多处重复出现）
- $(C(35,2))$

## 2.2. Training the artificial neural network (ANN) model

### 2.2.2 Transformer architecture: A physics-motivated choice

**代码链接**：`ML_code/train/train.py#L60-L68`
```python
def transformer_model(input_shape, output_dim):
    inp = Input(shape=input_shape)
    x   = MultiHeadAttention(num_heads=8, key_dim=32)(inp, inp)
    x   = LayerNormalization(epsilon=1e-6)(x)
    x   = Flatten()(x)
    x   = Dense(512, activation='relu')(x)
    x   = Dropout(0.1)(x)
    out = Dense(output_dim, activation='linear')(x)
    return Model(inp, out)
```

**代码链接**：`ML_code/train/train.py#L83-L88`
```python
model_fold = transformer_model(input_shape=X.shape[1:], output_dim=feat_dim)
model_fold.compile(loss='mse', optimizer='adam')

model_fold.fit(X_train_fold, y_train_fold,
               epochs=500, batch_size=1, verbose=1,
               validation_data=(X_val_fold, y_val_fold))
```

**代码链接**：`FEM-generated_training_datasets/generation_log.txt#L155-L163`
```text
Input Dimensions: 9 × 4 = 36 features (flattened)
Output Dimensions: 9 × 4 = 36 features (reshaped)
Number of Attention Heads (num_heads): 8
Key Dimension (key_dim): 32
Dense Layer Units: 512
Dropout Rate: 0.1
Optimizer: Adam (default learning rate: 0.001)
Loss Function: Mean Squared Error (MSE)
Training Epochs: 500
```

公式：
- $9 \times 4$

$$
L = \frac{1}{N}\sum_{j = 1}^{N}(y_{j} - \hat{y}_{j})^{2}
$$

### 2.2.3 Hyperparameter optimization and model validation

**代码链接**：`FEM-generated_training_datasets/generation_log.txt#L74-L78`
```text
Training Set: 80% (505 cases)
Validation Set: 20% (126 cases)
Split Method: Simulation-level random split with seed = 42
No data leakage: All ROIs from same simulation kept together
```

**代码链接**：`ML_code/train/train.py#L90-L98`
```python
y_pred_val = model_fold.predict(X_val_fold, verbose=0)
y_pred_bin = (y_pred_val >= 0.5).astype(int).ravel()
y_true_bin = (y_val_fold >= 0.5).astype(int).ravel()

acc  = accuracy_score(y_true_bin, y_pred_bin)
prec = precision_score(y_true_bin, y_pred_bin, zero_division=0)
rec  = recall_score(y_true_bin, y_pred_bin, zero_division=0)
f1   = f1_score(y_true_bin, y_pred_bin, zero_division=0)
iou  = jaccard_score(y_true_bin, y_pred_bin, zero_division=0)
```

**代码链接**：`ML_code/train/train.py#L104-L107`
```python
fold_metrics = np.array(fold_metrics)
mean_metrics = fold_metrics.mean(axis=0)
std_metrics = fold_metrics.std(axis=0)
```

公式：
- $(80\%)$
- $(20\%)$
- $(9 \times 4)$
- $\sim 92\%$
- $\{TP, FP, TN, FN\}$
- $\pm$
- $8\%$
- $F_{1} = 0.9948$

$$
F_{1} = \frac{2TP}{2TP + FP + FN}
$$

## 3. Results & discussion

### 3.1. Predictive accuracy and generalization

**代码链接**：`README.md#L73-L73`
```text
- Domain dimensions: 200×80 (main), 200×5 (VSBC strips)
```

**代码链接**：`results_reproduction/fig7/crack_analysis.m#L16-L20`
```matlab
porosity = [0, 10, 20, 30, 40, 50]; % Porosity levels in %

% Initialize storage
tortuosity = zeros(1, length(files));
deviation = zeros(1, length(files));
```

**代码链接**：`results_reproduction/fig7/crack_analysis.m#L42-L69`
```matlab
% Calculate actual crack path length (sum of distances between consecutive points)
actual_length = 0;
for j = 1:length(cols_sorted)-1
    dx = cols_sorted(j+1) - cols_sorted(j);
    dy = rows_sorted(j+1) - rows_sorted(j);
    actual_length = actual_length + sqrt(dx^2 + dy^2);
end

% Calculate straight-line distance (from initiation to termination)
straight_line_distance = sqrt((cols_sorted(end) - cols_sorted(1))^2 + ...
                              (rows_sorted(end) - rows_sorted(1))^2);

% Calculate tortuosity
if straight_line_distance > 0
    tortuosity(i) = actual_length / straight_line_distance;
else
    tortuosity(i) = 1;
end

% Calculate deviation from centerline (horizontal reference)
% Use the mean row position as the reference line
mean_row = mean(rows_sorted);

% Calculate perpendicular distances from each point to the horizontal reference
perpendicular_distances = abs(rows_sorted - mean_row);

% Mean deviation from centerline
deviation(i) = mean(perpendicular_distances);
```

**代码链接**：`results_reproduction/fig8/IOU_loop.m#L10-L12`
```matlab
snapshotList = 0:10:50;
cellsPerStep = 3;
```

公式：
- $200 \times 80$
- $0\%$
- $1\%$
- $2\%$
- $3\%$
- $4\%$
- $5\%$
- $(9 \times 4)$
- $(200 \times 80)$
- $0\%$
- $(\tau)$
- $\tau = \frac{L_{\mathrm{actual}}}{L_{\mathrm{straight}}}$
- $L_{\mathrm{actual}}$
- $L_{\mathrm{straight}}$
- $(D)$
- $D = \frac{1}{n} \sum_{i = 1}^{n} |y_{i} - y_{\mathrm{ref}}|$
- $y_{i}$
- $y$
- $i^{\mathrm{th}}$
- $y_{\mathrm{ref}}$
- $n$
- $\tau \approx 1.14$
- $5\%$
- $D \approx 3.3$
- $0–2\%$
- $3-5\%$
- $5\%$
- $8.3\%$
- $5.5\%$
- $\approx 0.85$
- $\approx 0.76$

### 3.2. Baselines & task non-triviality

**代码链接**：`results_reproduction/fig11/IOU_loop.m#L10-L59`
```matlab
snapshotList = 1:1:4;
cellsPerStep = 3;

for s = 1:numel(snapshotList)
    idx = snapshotList(s);
    fem = readmatrix(sprintf('fem_results/FEM_%d_hole2.txt', idx));
    ml  = readmatrix(sprintf('ml_results/ML_%d_hole2.txt',  idx));

    % Logical mask: 1 = crack; 2 = hole (ignored)
    femCrack = fem == 1;
    mlCrack  = ml  == 1;

    [nRow, nCol] = size(fem);
    orderFEM = [];
    orderML  = [];

    for c = 1:nCol
        rF = find(femCrack(:,c));
        rM = find(mlCrack(:,c));
        if ~isempty(rF)
            orderFEM = [orderFEM; sub2ind([nRow, nCol], rF, c*ones(size(rF)))];
        end
        if ~isempty(rM)
            orderML = [orderML; sub2ind([nRow, nCol], rM, c*ones(size(rM)))];
        end
    end

    totalSteps = ceil(max(numel(orderFEM), numel(orderML)) / cellsPerStep);
    iou = zeros(totalSteps,1);

    for t = 1:totalSteps
        N = cellsPerStep * t;
        setFEM = orderFEM(1:min(N,end));
        setML  = orderML( 1:min(N,end));
        iou(t) = numel(intersect(setFEM,setML)) / max(numel(union(setFEM,setML)),1);
    end
end
```

**代码链接**：`results_reproduction/fig11/IOU_loop.m#L97-L106`
```matlab
meanIoUs = zeros(size(snapshotList));
minIoUs  = zeros(size(snapshotList));

for s = 1:numel(snapshotList)
    idx = snapshotList(s);
    data = readmatrix(sprintf('IoU_data_%d.txt', idx));
    iouVals = data(:,2);  % second column = IoU
    meanIoUs(s) = mean(iouVals);
    minIoUs(s)  = min(iouVals);
end
```

公式：
- $9 \times 4$
- $\approx 0.85$
- $\approx 0.76$
- $\approx 0.43$
- $\approx 0.42$
- $\approx 0.68$
- $\approx 0.89$
- $\approx 0.12$
- $0.16$
- $0.32$
- $0.75$
- $\approx 31\%$
- $0.89$
- $0.68$
- $\sim 2.3\times$
- $0.75$
- $0.32$
- $\sim 5–6\times$

### 3.3. Discontinuity size effect

**代码链接**：`results_reproduction/fig13/3x3/ml_crack_path.m#L14-L29`
```matlab
initialCase1 = 4;
finalCase1 = 4;
case_interval1 = 10;

for j = initialCase1:case_interval1:finalCase1

data_file2 = sprintf('map0/map0_%d.txt',j);
data_fem0 = readmatrix(data_file2, 'NumHeaderLines', 0);

data_file0 = sprintf('ml_results/%d.txt',j);
data_ml = readmatrix(data_file0, 'NumHeaderLines', 0);
```

**代码链接**：`results_reproduction/fig13/4x4/ml_crack_path.m#L14-L29`
```matlab
initialCase1 = 4;
finalCase1 = 4;
case_interval1 = 10;

for j = initialCase1:case_interval1:finalCase1

data_file2 = sprintf('map0/map0_%d.txt',j);
data_fem0 = readmatrix(data_file2, 'NumHeaderLines', 0);

data_file0 = sprintf('ml_results/%d.txt',j);
data_ml = readmatrix(data_file0, 'NumHeaderLines', 0);
```

公式：
- $1 \times 1$
- $1 \times 1$
- $1 \times 1$
- $1 \times 1$
- $2 \times 2$
- $3 \times 3$
- $4 \times 4$
- $5\%$
- $2 \times 2$
- $5\%$
- $1 \times 1$
- $2 \times 2$
- $1 \times 1$
- $1 \times 1$
- $2 \times 2$
- $3 \times 3$
- $4 \times 4$
- $2 \times 2$

### 3.4. Probing the limits of extrapolation and the role of dataset richness

**代码链接**：`FEM-generated_training_datasets/generation_log.txt#L82-L89`
```text
DATASET 2: EXTENDED TRAINING SET (7176 CASES)
================================================================================
Description: Extended dataset including 0, 1, 2, and 3 pore configurations
Total Cases: 7176 (1 + 35 + 595 + 6545)
ML Dataset Size: 14352 matrices (input + output pairs)
Maximum Local Porosity: 8.3% (3 pores in 9×4 ROI)
```

**代码链接**：`results_reproduction/fig14/crack_path.m#L21-L28`
```matlab
data_file1 = sprintf('fem_results/%d.txt',j);
data_fem = readmatrix(data_file1, 'NumHeaderLines', 0);

data_file2 = sprintf('fem_results/map0_%d.txt',j);
data_fem0 = readmatrix(data_file2, 'NumHeaderLines', 0);

data_file0 = sprintf('ml_results/%d.txt',j);
data_ml = readmatrix(data_file0, 'NumHeaderLines', 0);
```

公式：
- $10\%$
- $17\%$
- $8.3\%$
- $10\%$
- $10\%$
- $\sim 0.46$
- $\sim 0.11$

## 4. Conclusions

**代码链接**：`FEM-generated_training_datasets/generation_log.txt#L12-L36`
```text
ROI Dimensions: 9 × 4 (elements)
Available Pore Locations: 35 discrete positions
Domain Size (FEM): 11 × 6 (elements)
Initial Crack Length: 2 elements
Loading Type: Mode I (Uniaxial tension along y-axis)
Master Random Seed: 42 (used for all stochastic processes)

DATASET 1: BASELINE TRAINING SET (631 CASES)
================================================================================
Description: Systematic enumeration of 0, 1, and 2 pore configurations
Total Cases: 631 (1 + 35 + 595)
ML Dataset Size: 1262 matrices (input + output pairs)
Maximum Local Porosity: 5.5% (2 pores in 9×4 ROI)
```

**代码链接**：`FEM-generated_training_datasets/generation_log.txt#L82-L89`
```text
DATASET 2: EXTENDED TRAINING SET (7176 CASES)
================================================================================
Description: Extended dataset including 0, 1, 2, and 3 pore configurations
Total Cases: 7176 (1 + 35 + 595 + 6545)
ML Dataset Size: 14352 matrices (input + output pairs)
Maximum Local Porosity: 8.3% (3 pores in 9×4 ROI)
```

公式：
- $9 \times 4$
- $5\%$
- $4 \times 4$
- $1 \times 1$
- $8.3\%$
- $10\%$
