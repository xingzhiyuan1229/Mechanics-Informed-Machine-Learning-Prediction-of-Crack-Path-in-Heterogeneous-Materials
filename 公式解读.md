# 公式解读

本文档按《Mechanics-informed machine learning prediction of crack path in heterogeneous materials》的原始结构分段，对文献.md中出现的全部公式与符号逐一解释，给出必要推导，并说明公式之间的内在联系。所有数学表达式均采用 $...$ 或 $$...$$ 形式渲染。

## ARTICLE INFO

本节为关键词与文章信息，未包含公式或数学符号。

## ABSTRACT

摘要为文字描述，未包含公式或数学符号。

## 1. Introduction

引言部分主要为背景论述，未给出独立公式或数学表达式。

## 2. Computational framework

本节建立相场断裂理论的能量表述，并引入变量刚度边界条件（VSBC）。公式 (1)–(3) 彼此递进：由 Griffith 型“尖裂纹”能量泛函出发，经 Ambrosio–Tortorelli 正则化得到相场形式，再得到包含退化弹性能与正则化裂纹能的总势能。之后引入 VSBC 的空间变模量公式，并与能量释放率 $J$ 的稳定性相联系。

### 2.0 相场断裂能量框架

**（1）尖裂纹能量泛函**

$$
\mathcal{E}(\mathbf{u},\Gamma) = \int_{\Omega \backslash \Gamma}\psi (\epsilon (\mathbf{u}))\mathrm{d}\mathbf{x} + G_{c}\mathcal{H}^{d - 1}(\Gamma) \qquad (1)
$$

- $\Omega \subset \mathbb{R}^d$ 表示材料所在的 $d$ 维空间域；$d$ 可以为 $2$ 或 $3$，对应平面问题或三维问题。
- $\mathcal{E}(\mathbf{u},\Gamma)$ 表示系统总能量（势能）泛函。
- $\Gamma$ 是未知的裂纹集合（裂纹路径/裂纹面）。
- $\mathbf{u}$ 为位移场；$\epsilon(\mathbf{u})$ 为位移场诱导的小应变张量。
- $\psi(\epsilon(\mathbf{u}))$ 是弹性能密度，描述材料因形变而储存的能量。
- $\mathcal{H}^{d-1}$ 为 $(d-1)$ 维 Hausdorff 测度，代表裂纹在 $d$ 维中所占的长度（$d=2$）或面积（$d=3$）。
- $G_c$ 为材料断裂韧度（临界能量释放率），表征单位裂纹面积的断裂能耗。

**推导要点与内在联系**：Griffith 理论认为裂纹演化遵循能量最小原则，总能量由“弹性能 + 裂纹表面能”构成；式 (1) 将这一思想写为泛函形式。后续相场模型通过将裂纹几何 $\Gamma$ 用连续损伤场逼近，从而把式 (1) 转化为可在固定网格上求解的正则化能量（见式 (2)、(3)）。

**（2）相场正则化裂纹能量（Ambrosio–Tortorelli 近似）**

$$
G_{c}\int_{\Gamma}\mathrm{d}H^{d - 1}\approx \min_{d}\int_{\Omega}G_{c}\left(\frac{d^{2}}{2\ell_{0}} +\frac{\ell_{0}}{2}|\nabla d|^{2}\right)\mathrm{d}\mathbf{x}. \qquad (2)
$$

- $d(\mathbf{x})\in [0,1]$ 是相场（损伤）变量：$d(\mathbf{x})=0$ 表示完好材料，$d(\mathbf{x})=1$ 表示完全开裂；介于 $0$ 与 $1$ 的区域为弥散裂纹带。
- 文中在式 (2) 前将裂纹能量泛函记作 $\mathcal{G}(\Gamma)$，此处等价写成 $G_c\int_{\Gamma}\mathrm{d}H^{d-1}$。
- $\ell_0$ 为相场内禀长度尺度，控制弥散裂纹带宽度。
- $\ell_0\to 0$ 时，右侧正则化能量在 $\Gamma$-收敛意义下趋近左侧尖裂纹表面能；这里的 $\Gamma$-收敛是一种变分意义的收敛方式，保证能量泛函的极小值与极小点在极限过程中与尖裂纹模型一致。
- 正则化项由“局部惩罚” $\frac{d^2}{2\ell_0}$ 与“梯度惩罚” $\frac{\ell_0}{2}|\nabla d|^2$ 组成：前者鼓励 $d$ 接近 $0$ 或 $1$，后者限制 $d$ 的空间变化，从而形成有限宽度的裂纹带。

**推导要点与内在联系**：式 (2) 用连续场 $d(\mathbf{x})$ 取代尖裂纹几何 $\Gamma$，使能量表达式可以在全域 $\Omega$ 上积分。式 (1) 的裂纹能量项被式 (2) 替代，从而为式 (3) 的总能量表达奠定基础。

**（3）正则化后的总势能**

$$
E_{\ell_0}(\mathbf{u},d) = \int_{\Omega}g(d)\psi (\epsilon (\mathbf{u}))\mathrm{d}\mathbf{x} + \int_{\Omega}G_c\left(\frac{(1 - d)^2}{2\ell_0} +\frac{\ell_0}{2}|\nabla d|^2\right)\mathrm{d}\mathbf{x} \qquad (3)
$$

- $E_{\ell_0}(\mathbf{u},d)$ 表示正则化后的总势能；其自变量为位移场与相场损伤场 $(\mathbf{u},d)$。
- 第一项是退化弹性能：通过 $g(d)$ 对 $\psi$ 进行削弱，反映裂纹区刚度退化。
- 第二项为正则化裂纹能量。式 (2) 中的 $d$ 按常规定义为损伤度（$0$ 完好、$1$ 破坏），式 (3) 则采用完整度记法 $\tilde d = 1-d$，因此以 $(1-d)^2/(2\ell_0)$ 表达裂纹密度项；二者通过变量替换 $\tilde d = 1-d$ 相互对应。

与式 (3) 相关的关键子公式如下：

- 弹性能密度（线弹性）
  
  $\psi(\epsilon) = \frac{1}{2}\epsilon :\mathbb{C}:\epsilon$
  
  其中 $\mathbb{C}$ 为弹性刚度张量，$:$ 表示双点积。

- 退化函数
  
  $g(d)$ 为损伤退化函数，常取
  
  $g(d) = (1 - d)^2 + k$，其中 $k\ll 1$ 为残余刚度，用于避免完全破坏区域的数值奇异性。

- 应力定义
  
  $\sigma = g(d)\frac{\partial\psi}{\partial\epsilon}$，表明应力在裂纹区随 $g(d)$ 退化。

- 损伤不可回复约束
  
  $\dot{d}(\mathbf{x},t)\geq 0$ 表示随时间裂纹不可自愈，其中 $\dot{d} = \partial d/\partial t$；在离散步 $n$ 下体现为 $d_{n+1} \ge d_n$。

**推导要点与内在联系**：式 (3) 由“式 (1) 的弹性能 + 式 (2) 的裂纹能量”组合得到。$g(d)$ 使材料刚度随裂纹增长而降低，从而把裂纹几何的影响嵌入能量中。通过对 $E_{\ell_0}(\mathbf{u},d)$ 进行变分，可得到位移场与损伤场的耦合 Euler–Lagrange 方程，这是后续数值求解与数据生成的物理基础。

### 2.0.1 变量刚度边界条件（VSBC）

- 域划分：主域为 $\Omega_{\mathrm{d}}$，变量刚度条带域为 $\Omega_{\mathrm{vsd}}$。该符号区分了材料主体与施加特殊边界处理的附加区域。

- 刚度空间分布：

  $E_{\mathrm{vsd}} = a\left(1 - \frac{x}{L_x}\right)^n + b$

  其中 $x$ 为沿加载方向的坐标，$L_x$ 为该方向尺度长度，$n=8$、$a=0.4$、$b=0.005$ 为经验参数。若取 $x=0$ 为加载端、$x=L_x$ 为远端，则 $(1-x/L_x)^n$ 在加载端取 $1$、在远端取 $0$，对应刚度由大到小平滑变化，从而将均匀位移转化为非均匀位移边界。

- 能量释放相关量：文中用 $J$ 表示 $J$-积分，它等价于裂尖能量释放率。曲线段标记 $a-b$、$b-c$、$c-d$ 对应文献 Fig. 2 (Right) 中的 $J$-积分—时间步曲线不同阶段（起裂增长、动态跳变、稳定扩展）。

- 数值参数：$G_c$ 为材料断裂韧度；模拟中取 $\ell_0 = 0.6$、网格尺寸 $h=0.25$。这些参数保证相场裂纹带与网格尺度相匹配，避免数值伪影。

**内在联系**：VSBC 的设计目标是使裂尖能量释放率 $J$ 维持在接近 $G_c$ 的水平，实现稳定裂纹扩展。这一稳定性保证了相场能量框架（式 (1)–(3)）可生成质量稳定的裂纹轨迹数据，从而支撑后续 ML 数据集构建。

### 2.1 Generation of ML datasets

本节公式主要描述几何尺度、应力场与 ROI（感兴趣区域）的设置，保证 ML 数据集中包含物理上关键的裂尖影响区。

- 控制域大小：示例有限元域为 $11 \times 6$；ROI 尺寸为 $9 \times 4$。这两个尺寸决定了输入/输出矩阵的几何意义与尺寸。

- 应力分量：$\sigma_{yy}$ 表示 $y$ 方向的正应力分量；高 $\sigma_{yy}$ 区域对应 Mode I 裂纹尖端的主要驱动区。

- 稳定步长：在 VSBC 下裂纹每步稳定增长约 $4$ 个单位（通常以网格单元计；若以物理长度计，则约为 $4h$），因此 ROI 在裂纹推进方向上的尺寸取 $4$（即 $9 \\times 4$ 的短边），与每步增长尺度相匹配，以保证每一步输入都覆盖对下一步裂纹演化最敏感的区域。

### 2.2. Training the artificial neural network (ANN) model

#### 2.2.1 Dataset construction and sampling strategy

- ROI 尺寸仍为 $9 \times 4$，其中共有 $35$ 个可放置孔洞的位置。

- 组合数：两孔洞组合数写为 $C(35,2)$。其推导为：
  
  $C(35,2) = \frac{35\times 34}{2} = 595$
  
  因此总样本数为 $1$（无孔）$+35$（单孔）$+595$（双孔）$=631$。

- 数据矩阵数：每个 FEM 仿真产生输入与输出各一张矩阵，因此共有 $2\times 631 = 1262$ 个 $9 \times 4$ 数值矩阵。

**内在联系**：几何采样策略保证 ML 模型见到的局部裂尖-孔洞构型覆盖完备，从而与相场能量产生的物理裂纹路径相一致。

#### 2.2.2 Transformer architecture: A physics-motivated choice

- 输入向量维度：$9 \times 4$ ROI 被展平成 $36$ 维向量，这保证输入特征与空间像素一一对应。
- 注意力层参数：$\text{num\_heads}=8$、$\text{key\_dim}=32$，多头注意力用于建模非局部裂尖-孔洞相互作用。
- 全连接层规模：$\text{Dense}=512$；输出层为 $36$ 维，最后再重塑为 $9 \times 4$ 预测矩阵。
- 正则化：Dropout 率为 $0.1$，用于抑制过拟合。

**损失函数（均方误差）**

$$
L = \frac{1}{N}\sum_{j = 1}^{N}(y_{j} - \hat{y}_{j})^{2}
$$

- $N$ 为输出像素总数（此处 $N=36$），$y_j$ 为真实标签像素，$\hat{y}_j$ 为模型预测。
- 该式由“最小化平方误差”原则得到，是回归/分割任务中对几何差异的直接度量。

**数据划分**

- 训练/验证比例分别为 $80\%$ 与 $20\%$，且按仿真样本分组避免数据泄漏。

#### 2.2.3 Hyperparameter optimization and model validation

- 类别不平衡：完好像素约占 $\sim 92\%$，裂纹/孔洞像素约占 $8\%$。
- 混淆矩阵记为 $\{TP, FP, TN, FN\}$。

**F1 指标公式**

$$
F_{1} = \frac{2TP}{2TP + FP + FN}
$$

- 该式由精确率与召回率的调和平均推导得到，对正类稀疏问题更敏感。
- 文中给出 $F_{1} = 0.9948$，说明在稀疏裂纹像素上同时取得高精确率与高召回率。

**交叉验证的“均值 ± 标准差”**

- $\pm$ 表示“均值加减标准差”，即 $\text{mean} \pm \text{std}$，用于刻画多折交叉验证的波动范围。

## 3. Results & discussion

### 3.1. Predictive accuracy and generalization

- 预测域规模为 $200 \times 80$，孔隙率分别为 $0\%$、$1\%$、$2\%$、$3\%$、$4\%$、$5\%$。
- 测试时依旧以 $(9 \times 4)$ 的局部 ROI 进行自回归预测。

**曲折度（tortuosity）**

- 记号 $\tau$ 表示曲折度，其定义为
  
  $\tau = \frac{L_{\mathrm{actual}}}{L_{\mathrm{straight}}}$
  
  其中 $L_{\mathrm{actual}}$ 为真实裂纹路径长度，$L_{\mathrm{straight}}$ 为起止点间直线距离。该式体现“路径越弯曲，实际长度越长”。

**中心线偏移**

- 偏移量 $D$ 定义为
  
  $D = \frac{1}{n} \sum_{i = 1}^{n} |y_{i} - y_{\mathrm{ref}}|$
  
  其中 $y_i$ 为第 $i$ 个裂纹像素的 $y$ 坐标，$y_{\mathrm{ref}}$ 为参考中心线坐标，$n$ 为裂纹像素总数。

- 结果层面：在 $5\%$ 孔隙率时，$\tau \approx 1.14$ 与 $D \approx 3.3$。若取 $\tau = 1.14$，则相对增量为 $(\tau-1)\times 100\% = 14\%$；若 $\tau$ 仅近似为 $1.14$，该比例可写为约 $14\%$。$D \approx 3.3$ 表示中心线偏移约 $3.3$ 像素。
- 低孔隙率区间 $0\%\text{–}2\%$ 与高孔隙率区间 $3\%\text{–}5\%$ 的对比显示，随着孔隙率上升，裂纹路径的几何复杂性显著增强。

**局部超出训练分布**

- 文中指出 ROI 内局部孔隙率达到 $8.3\%$，而训练上限为 $5.5\%$，说明模型在超出训练分布时仍能利用注意力机制进行组合泛化。

### 3.2. Baselines & task non-triviality

本节给出 IoU 统计值及相对改进比例，均以近似符号表达：

- Transformer 的中后期 IoU 约为 $\approx 0.85$，末期约为 $\approx 0.76$。
- 基线模型的平均 IoU 分别为 $\approx 0.43$、$\approx 0.42$、$\approx 0.68$，Transformer 为 $\approx 0.89$。
- 最小 IoU 约为 $\approx 0.12$、$0.16$、$0.32$，Transformer 为 $0.75$。
- 相对提升：平均 IoU 提升约 $\approx 31\%$（$0.89$ 相对 $0.68$），最小 IoU 提升约 $\approx 2.3\times$（$0.75$ 相对 $0.32$），并相对简单基线提升约 $\approx 5\text{--}6\times$。

这些数值说明：在相同 ROI 自回归框架下，Transformer 对误差累积的抑制能力显著优于基线模型。

### 3.3. Discontinuity size effect

- 孔洞尺寸依次为 $1 \times 1$、$2 \times 2$、$3 \times 3$、$4 \times 4$，均保持全局孔隙率约 $5\%$。
- 文中强调以 $1 \times 1$ 数据训练的模型，在 $2 \times 2$、$3 \times 3$、$4 \times 4$ 的更大孔洞下仍能保持合理预测，但误差随孔洞尺寸增加而累积更快。

### 3.4. Probing the limits of extrapolation and the role of dataset richness

- 在 $10\%$ 全局孔隙率下，局部 ROI 出现 $17\%$ 孔隙率，远超训练上限 $8.3\%$。
- 扩充数据集后，最终 IoU 由 $\sim 0.11$ 提升至 $\sim 0.46$，表明增加高复杂度样本可显著改善 OOD 泛化。

## 4. Conclusions

结论部分对主要结果进行回顾，并再次出现以下数学表达：

- ROI 尺寸为 $9 \times 4$，这是模型关注裂尖过程区的核心尺度。
- 模型对 $5\%$ 孔隙率内的裂纹预测具有高准确性；通过丰富数据集还能覆盖局部 $8.3\%$ 及全局 $10\%$ 的更高孔隙率。
- 模型从 $1 \times 1$ 孔洞训练出发，在预测 $4 \times 4$ 更大孔洞时仍保持合理表现。

## Data availability

本节为数据可用性声明，未包含公式。

## Declaration of competing interest

本节为利益冲突声明，未包含公式。

## Appendix A

附录为代码公开链接说明，未包含公式。

## References

参考文献为引文列表，未包含公式。
